# Frontend (painel React) — único env necessário no build do painel.
# URL pública do backend FastAPI.
VITE_PIZZABOT_API_URL="https://api.seu-dominio.com"

# Para o ambiente do BACKEND (FastAPI/Celery), veja backend/.env.example
# (DATABASE_URL, REDIS_URL, EVOLUTION_BASE_URL, EVOLUTION_API_KEY,
#  GEMINI_API_KEY, APP_SECRET_KEY, PUBLIC_BASE_URL, APP_ROLE, ...).
